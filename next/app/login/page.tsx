import LoginClient from './LoginClient'

export const metadata = {
  title: 'Login — AfriDigest',
  description: 'Log in to your AfriDigest account.',
}

export default function LoginPage(){
  return <LoginClient />
}
